# WASI 0.2 WIT

The WIT definitions for WASIp2. Currently at v0.2.12

Sourced from: https://github.com/WebAssembly/WASI/releases/tag/v0.2.12

This directory is the contents of the `wasi:cli` package (`cli` subdirectory) only.